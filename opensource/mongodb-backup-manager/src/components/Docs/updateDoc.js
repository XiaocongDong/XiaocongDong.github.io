import React from 'react';

export default {
    title: 'update an existing backup config',
    content: (
        <div>
            <p>
                After submitting the backup schedule, you can update the
                configuration whenever you want. 
                To update your backup configuration,
                you need to click the 'Edit Config' button on the configurations page of the specific backup,
                make any changes and then sumit the configuration again.
            </p>
            <img src="./static/img/update_config.png"/>
        </div>
    )
}